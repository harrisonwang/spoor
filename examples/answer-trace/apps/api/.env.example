# ── 模型后端:任意 OpenAI 兼容端点 ──────────────────────────────
# 方式一:Cloudflare Workers AI(默认,有免费额度 10,000 neurons/天)
#   到 dash.cloudflare.com/profile/api-tokens 建一个有 "Workers AI" 权限的 Token。
CF_ACCOUNT_ID=
CF_API_TOKEN=

# 方式二:直接指定任意 OpenAI 兼容端点(设了 AT_BASE_URL 就优先用,覆盖 CF)
#   OpenRouter: https://openrouter.ai/api/v1
#   DeepSeek:   https://api.deepseek.com
#   z.ai:       https://api.z.ai/api/paas/v4
# AT_BASE_URL=https://openrouter.ai/api/v1
# AT_API_KEY=sk-...

# ── 模型(按所选后端填对应 ID)──────────────────────────────────
# 默认是 Cloudflare 的 ID;换后端记得改成该平台的模型名。
# AT_GEN_MODEL=@cf/google/gemma-4-26b-a4b-it
# AT_JUDGE_MODEL=@cf/qwen/qwen3-30b-a3b-fp8

# ── 上下文上限(token):仅用于前端"是否超限"提示,按所选模型设 ──
# AT_CONTEXT_LIMIT=8192
