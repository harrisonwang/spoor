# Chat with a local file

This offline example parses a local document in WASM and ranks matching
paragraphs without uploading the file.

```bash
cd crates/spoor-wasm
npm run build:web
cd ../../examples/chat-with-local-file
npm run dev
```

Open `/examples/chat-with-local-file/` on the printed local URL.
