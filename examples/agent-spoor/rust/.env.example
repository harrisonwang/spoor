# 任意 OpenAI 兼容端点（OpenRouter / DeepSeek / z.ai / Cloudflare Workers AI …）
BASE_URL=https://openrouter.ai/api/v1
OPENAI_API_KEY=
OPENAI_MODEL=

# 可选：skill 模式用的 spoor CLI 路径（默认走 PATH 上的 `spoor`）
# SPOOR_BIN=spoor
