# @harrisonwang/pith-darwin-arm64

Platform-specific binary package for [pith](https://github.com/harrisonwang/pith). Do not install directly — install the main package `@harrisonwang/pith`, which pulls in the matching binary via optional dependencies.
